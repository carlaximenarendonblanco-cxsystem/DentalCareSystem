APP_NAME=DentalCare
APP_ENV=production
APP_KEY=base64:nwhJ1ZzSz0Wla3tyuBzwfhqvGwxyhdCLFSPvoVlnQak=
APP_URL=https://dentalcaresystem-6lbt.onrender.com
APP_DEBUG=false

# -----------------------------------------------------------------
# CONFIGURACIÓN CRÍTICA DE SESIÓN (Solución para el error 419)
# -----------------------------------------------------------------
# Dejar SESSION_DOMAIN vacío/null permite que Laravel lo determine automáticamente en Render.
SESSION_DOMAIN=
# CRÍTICO: Debe ser TRUE ya que APP_URL es HTTPS
SESSION_SECURE_COOKIE=true
# Usar driver 'file' en Docker si no configuras Redis/DB para sesiones
SESSION_DRIVER=file
SESSION_LIFETIME=120

LOG_CHANNEL=stack
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

# -----------------------------------------------------------------
# BASE DE DATOS (PostgreSQL)
# -----------------------------------------------------------------
DB_CONNECTION=pgsql
DB_HOST=dpg-d4etebbgk3sc73bv84ig-a.oregon-postgres.render.com
DB_PORT=5432
DB_DATABASE=dentalcarecx
DB_USERNAME=user_dcx
DB_PASSWORD=FIi0yFycF5IuTc0TSc0GjBZCeMAprVq8

# -----------------------------------------------------------------
# CACHE Y COLAS (Drivers recomendados para Docker)
# -----------------------------------------------------------------
BROADCAST_DRIVER=log
CACHE_DRIVER=file 
FILESYSTEM_DISK=local
QUEUE_CONNECTION=sync # Usar 'sync' si no tienes un servicio de cola dedicado

# -----------------------------------------------------------------
# MAIL (Configuración de ejemplo)
# -----------------------------------------------------------------
MAIL_MAILER=smtp
MAIL_HOST=smtp.mailtrap.io # Cambia esto a tu host real (ej: Brevo, SendGrid)
MAIL_PORT=587
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_ENCRYPTION=tls
MAIL_FROM_ADDRESS="no-reply@dentalcare.com"
MAIL_FROM_NAME="${APP_NAME}"

# -----------------------------------------------------------------
# OTROS (Dejar como están si no los usas)
# -----------------------------------------------------------------
MEMCACHED_HOST=127.0.0.1
REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379

AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false

PUSHER_APP_ID=
PUSHER_APP_KEY=
PUSHER_APP_SECRET=
PUSHER_HOST=
PUSHER_PORT=443
PUSHER_SCHEME=https
PUSHER_APP_CLUSTER=mt1

VITE_PUSHER_APP_KEY="${PUSHER_APP_KEY}"
VITE_PUSHER_HOST="${PUSHER_HOST}"
VITE_PUSHER_PORT="${PUSHER_PORT}"
VITE_PUSHER_SCHEME="${PUSHER_SCHEME}"
VITE_PUSHER_APP_CLUSTER="${PUSHER_APP_CLUSTER}"